import style from './Cart.module.css';
    
interface CartProps {
}
    
export const Cart = ({ }: CartProps) => {
//
    return (
        <div className={style.cart}>
    
            /
        </div>
    );
};